

PORT=5000

# Update user/password/database name to match what have been created in pgAdmin/psql
DATABASE_URL=postgresql://postgres:YOUR_DB_PASSWORD@localhost:5432/smart_hostel_finder

# Any long random string — used to sign login tokens
JWT_SECRET=this_will_be_changed_to_a_long_random_secret_string
